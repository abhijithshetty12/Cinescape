# TODO - Profile Recommendations Upgrade

- [ ] Update `RecommendationSection` to return up to **50** items instead of 20.
- [ ] Add TMDB **Trending (week)** + **Popular** sources into the recommendation mix for both movies and series.
- [ ] Add periodic auto-refresh (every **60s**) for `RecommendationSection` while the user is on the page.
- [ ] Ensure exclude rules (already watched + in watchlist) are applied after merging all sources.
- [ ] Verify build/test passes.

